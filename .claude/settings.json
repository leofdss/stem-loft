{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/cargo_check_on_rust_edit.py\" ]; then python3 \"$root/.claude/hooks/cargo_check_on_rust_edit.py\"; fi",
            "statusMessage": "Running cargo check...",
            "timeout": 60
          },
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/readme_sync_reminder.py\" ]; then python3 \"$root/.claude/hooks/readme_sync_reminder.py\"; fi",
            "statusMessage": "Checking README is still in sync..."
          },
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/pt_br_sync_reminder.py\" ]; then python3 \"$root/.claude/hooks/pt_br_sync_reminder.py\"; fi",
            "statusMessage": "Checking pt-BR sibling is still in sync..."
          },
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/kanban_sync_reminder.py\" ]; then python3 \"$root/.claude/hooks/kanban_sync_reminder.py\"; fi",
            "statusMessage": "Checking kanban board is still in sync..."
          },
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/doc_links_check.py\" ]; then python3 \"$root/.claude/hooks/doc_links_check.py\"; fi",
            "statusMessage": "Checking internal doc links..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$root\" ] && [ -f \"$root/.claude/hooks/bash_guard.py\" ]; then python3 \"$root/.claude/hooks/bash_guard.py\"; fi",
            "statusMessage": "Checking project rules..."
          }
        ]
      }
    ]
  }
}
